version: '3.8'

services:
  db:
    image: postgres:latest
    container_name: eduvial-db
    environment:
      POSTGRES_USER: EduVial-team
      POSTGRES_PASSWORD: tu_contraseña
      POSTGRES_DB: nombre_basedatos
    ports:
      - "5432:5432"
    networks:
      - eduvial-network

  backend:
    build: .
    container_name: eduvial-backend
    environment:
      DB_HOST: db
      DB_USER: EduVial-team
      DB_PASSWORD: tu_contraseña
      DB_NAME: nombre_basedatos
    ports:
      - "3000:3000"
    depends_on:
      - db
    networks:
      - eduvial-network

networks:
  eduvial-network:
    driver: bridge
